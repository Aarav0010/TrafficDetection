name: Build Jetson GPU Docker Image

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    
    steps:
      - name: Free Disk Space (Ubuntu)
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true

      - name: Checkout repository
        uses: actions/checkout@v4

      # Set up QEMU for multi-architecture builds (ARM64 emulation)
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      # Set up Docker Buildx for cross-compilation
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Login to GitHub Container Registry
      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Build the ARM64 image and export as tarball
      - name: Build and export Jetson GPU Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.gpu
          platforms: linux/arm64
          tags: traffic-detection-gpu:latest
          outputs: type=docker,dest=traffic-detection-gpu.tar
          cache-from: type=gha
          cache-to: type=gha,mode=max

      # Upload the tar file as a GitHub artifact
      - name: Upload GPU Docker image artifact
        uses: actions/upload-artifact@v4
        with:
          name: jetson-gpu-docker-image
          path: traffic-detection-gpu.tar
          retention-days: 5
